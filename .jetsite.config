# Jetsite Configuration
# This file contains default settings for the Jetsite script
# You can modify these values to customize the behavior

# Default template repository (format: owner/repository-name)
DEFAULT_TEMPLATE_REPO="owner/template-repo"

# Default repository visibility (public or private)
DEFAULT_VISIBILITY="public"

# Whether to automatically open VS Code after cloning
AUTO_OPEN_VSCODE=true

# Default clone directory (relative to current working directory)
# Leave empty to clone in current directory
CLONE_DIRECTORY=""

# Additional git configuration
# These will be applied to the cloned repository
GIT_USER_NAME=""
GIT_USER_EMAIL=""

# Custom post-creation commands
# Commands to run after repository creation and cloning
# Separate multiple commands with semicolons
POST_CREATION_COMMANDS=""

# Examples:
# POST_CREATION_COMMANDS="npm install; npm run dev"
# POST_CREATION_COMMANDS="pip install -r requirements.txt; python manage.py migrate"
# POST_CREATION_COMMANDS="bundle install; rails server"
